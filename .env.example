# ==============================================================================
# ENVIRONMENT VARIABLES EXAMPLE
# ==============================================================================
# This file documents all environment variables needed by the application
# 
# SETUP INSTRUCTIONS:
# 1. Copy this file: cp .env.example .env
# 2. Fill in your actual values in .env
# 3. NEVER commit .env to git (it's in .gitignore)
# 4. Docker Compose automatically loads .env file
# ==============================================================================

# ------------------------------------------------------------------------------
# API Keys (REQUIRED)
# ------------------------------------------------------------------------------
# OpenAI API Key - Get from https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# GitHub Token (optional) - Only needed if using GitHub-hosted models
# Get from https://github.com/settings/tokens
GITHUB_TOKEN=ghp_your-github-token-here

# ------------------------------------------------------------------------------
# Model Configuration (OPTIONAL - has defaults)
# ------------------------------------------------------------------------------
# Primary model for decision-making agents
MODEL_NAME=gpt-4o-mini

# Model for evaluation agents
EVALUATION_MODEL_NAME=gpt-4o-mini

# Available models:
# - gpt-4o-mini (fast, cheap, good quality)
# - gpt-4o (slower, expensive, best quality)
# - gpt-4-turbo
# - gpt-3.5-turbo (fastest, cheapest, lower quality)

# ------------------------------------------------------------------------------
# Redis Configuration (OPTIONAL - has defaults)
# ------------------------------------------------------------------------------
# Redis host (use 'redis' when running in Docker Compose)
REDIS_HOST=redis

# Redis port
REDIS_PORT=6379

# Redis database number
REDIS_DB=0

# Redis password (if you enable authentication)
# REDIS_PASSWORD=your-redis-password

# ------------------------------------------------------------------------------
# Application Settings (OPTIONAL - has defaults)
# ------------------------------------------------------------------------------
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# API title and version
API_TITLE=Multi-Agent Decision Making API
API_VERSION=0.2.0

# Enable/disable features
ENABLE_REDIS_PERSISTENCE=false  # Set to true when Redis persistence is implemented
